# 安全说明

本面板会保存并远程操作 VPS 凭据，因此安全相关的问题请通过 GitHub 的 Security Advisory 私下报告，不要写成公开 Issue。

## 安全边界

- 面板默认只监听本机或 Docker 绑定 `127.0.0.1`，不要暴露到公网。
- `data/.secret` 和服务器私钥等文件不要提交到 Git，也不要上传到任何公开位置。
- 面板当前没有登录鉴权，适合本机或受控网络使用。

## 修复承诺

- 可导致凭据泄露、远程命令执行或未授权访问的问题会优先处理。
- 修复后会在发布说明中标注，建议用户尽快升级。
